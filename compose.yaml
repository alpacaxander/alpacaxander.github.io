name: example-alpacaxander

services:
  example-api:
    build: ./example-data
    ports:
      - "8080:8080"

  example-ui:
    build: ./example-frontend
    env_file: "./example-frontend/.env.development.local"
    volumes:
      - ./example-frontend/src:/src
    ports:
      - "3000:3000"
