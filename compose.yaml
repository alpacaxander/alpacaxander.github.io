name: example-alpacaxander

services:
  example-api:
    build: ./example-data
    env_file: "./example-data/.env.development"
    ports:
      - "8080:8080"

  example-ui:
    build: ./example-frontend
    volumes:
      - ./example-frontend/src:/src
    ports:
      - "3000:3000"
